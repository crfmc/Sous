{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "esbuild-client": "npm --prefix ../client/ run esbuild",
    "esbuild": "esbuild scripts/App.jsx --bundle --platform=node --outfile=built/app.js --jsx-factory=h --jsx-fragment=Fragment",
    "nodemon": "nodemon --watch scripts server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"npm:esbuild\" \"npm:nodemon\" "
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.3.0",
    "esbuild": "^0.15.7",
    "express": "^4.18.1",
    "fs": "^0.0.1-security",
    "isomorphic-fetch": "^3.0.0",
    "nodemon": "^2.0.19",
    "path": "^0.12.7",
    "preact": "^10.11.0",
    "sass": "^1.54.4"
  },
  "devDependencies": {
    "preact-render-to-string": "^5.2.4"
  }
}